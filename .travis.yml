sudo: false
language: java
cache:
  directories:
    - "$HOME/.cache"
jdk:
  - openjdk8
env:
  - CODECOV_TOKEN="c59c9137-0695-4f67-90b4-0b79e8741b0a"

after_success:
  - bash <(curl -s https://codecov.io/bash)

notifications:
  email:
    recipients:
      - blech.kate@ya.ru
    on_success: change
    on_failure: always

##  mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V